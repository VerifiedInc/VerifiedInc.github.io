```typescript title="Example: Autofill, Inputs from Manual Signup, Minimal Inputs"
{
    // "externalReference": "appointment-1047" // optional: your own reference string, to be included in responses
    "fullName": {
        "firstName": "Richard",
        "lastName": "Hendricks"
    },
    "birthDate": "1989-08-01",
    "payer": { // Payer can be optionally included to autofill only for a specific payer (or payer group)
        "id": "VERIFIED_MEDICARE"
        // "name": "Verified Medicare" // Only use if payer ID is not known
    }
}
```
