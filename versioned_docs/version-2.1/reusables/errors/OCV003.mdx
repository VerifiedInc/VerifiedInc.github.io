import Admonition from '@theme/Admonition';
import ErrorDisplay from '@site/src/components/ErrorDisplay.jsx';

<ErrorDisplay
  message='Cannot resend code for this verification'
  attemptsRemaining='integer'
  expiresAt='integer'
  whenThisIsReturned='When the verification message cannot be delivered again because the verification status is verified, expired, or failed.' 
  howToHandle='Start a new verification flow.'
>
  ###### `OCV003`
</ErrorDisplay>