# PR build gate: the site must compile before merge. Runs without secrets so
# it also reports on Dependabot PRs — unlike the Vercel preview deploy, which
# fails on them for lack of a token — making it the required status check
# that gates Dependabot auto-merge. ref: Linear ENG-190
name: CI
on: pull_request

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run build
