import Admonition from '@theme/Admonition';
import DomainsAdmonition from '@site/versioned_docs/version-2/reusables/domains-admonition.mdx'

Append the `/v2` path to the Production base URL for your Verified API calls, and replace your old Production API key with the v2 Production API key for your brand. _You can copy this API key from the top of the Brand Details page in the Dashboard._

<table>
    <tr>
        <th>Version</th>
        <th>Base URL</th>
    </tr>
    <tr>
        <th>v1</th>
        <td><code><span>https://</span>core-api.verified<b>.inc</b></code></td>
    </tr>
    <tr>
        <th>v2</th>
        <td><code><span>https://</span>core-api.verified<b>.inc</b>/v2</code></td>
    </tr>
</table>

<DomainsAdmonition/>

<Admonition type="info">
    For you to be able to create Production API keys, we need to approve your customer account. If you need us to do so, reach out to us at Support@Verified.inc.
</Admonition>