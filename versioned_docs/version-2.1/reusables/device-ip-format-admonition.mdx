:::info Device IP Format
[`GET /1-click/verifications/device-ip`](/reference/api/endpoints#get-1-click-verifications-device-ip) always returns device IP in [IPv6](https://en.wikipedia.org/wiki/IPv6_address) format, because this is what our other endpoints require. We internally map IPv4 to IPv6 when necessary.
:::