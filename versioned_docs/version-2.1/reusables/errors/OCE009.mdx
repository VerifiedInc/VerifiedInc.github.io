import ErrorDisplay from '@site/src/components/ErrorDisplay.jsx';

<ErrorDisplay
  message='Verification code is required'
  identifiers='{ [identifierType: string]: string }'
  verificationMethod='{ [identifierType: string]: string }'
  description=''
  integrationType='Semi-Hosted (in v2.0)'
  whenThisIsReturned="When you don't include a user input for verification code calling GET /1-click."
  howToHandle='Prompt the user to input the verification code you sent them via SMS, and then call GET /1-click with that user input.'
  youShouldNeverGetThisError="If you're using the Semi-Hosted integration type, you should always call GET /1-click with a user input for verification code."
>
  ##### `OCE009`
</ErrorDisplay>