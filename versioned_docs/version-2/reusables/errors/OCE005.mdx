import ErrorDisplay from '@site/src/components/ErrorDisplay.jsx';

<ErrorDisplay
  message='User Credentials or Credential Requests are missing'
  description=''
  integrationType=''
  whenThisIsReturned='When Verified cannot find credentials or credential requests for the 1-Click flow.' 
  howToHandle='Fall back to a manual signup flow.'
  youShouldNeverGetThisError='Verified should always be able to use default credential requests and return credentials for a user or return another error code, like OCE013.'
>
  ##### `OCE005`
</ErrorDisplay>